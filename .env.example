# Path to your Claude Code data directory
# Defaults to ~/.claude if not set
CLAUDE_DIR=~/.claude

# Server port
PORT=3001

# Cache TTL in seconds (how long parsed data is cached in memory)
CACHE_TTL_SECONDS=60
